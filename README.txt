In order to run any of the visual parts of HotCiv,
the ant file must first run its 'copyResource' target.

To see the final product with the visuals, run
hotciv.visual.ShowIntegrationTool.java as a java
application. The file is within the 'test' folder.